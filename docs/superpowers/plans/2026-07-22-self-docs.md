# Self-docs для external executor'ов — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать внешнему агенту (Claude Code / OpenCode / Codex) доступ к документации Св_арога в sandbox, чтобы он отвечал на вопросы пользователя о самом Св_ароге по реальной доке.

**Architecture:** При старте контейнера контрол-плейн стейджит копию доков (`README.md`, `AGENTS.md`, `docs/adr/`) в launch-директорию run'а, генерит `INDEX.md`, монтирует папку `:ro` в `/opt/svarog-docs` через существующий `extra_mounts`, и встраивает указатель на путь в контекст-файл агента через `context_files()`. Файловый механизм (не MCP), т.к. MCP reverse-tools доходят только до Claude Code.

**Tech Stack:** Python 3.12, pytest / pytest-asyncio, pydantic (StrictModel), stdlib `shutil`/`pathlib`.

## Global Constraints

- Python 3.12+; типизация обязательна (mypy в CI), стиль ruff.
- Конфиг-модели — `StrictModel` (`extra="forbid"`): новые поля только с дефолтом.
- Мягкая деградация: недоступный docs-root НЕ роняет run.
- Монтирование только `:ro`, вне state volume; доки публичны (секретов нет).
- Комментарии и текст — на русском, как в окружающем коде.
- Спека: `docs/superpowers/specs/2026-07-22-self-docs-design.md`.

---

## File Structure

- **Create** `src/svarog_harness/runtime/self_docs.py` — резолвер docs-root, стейджинг, генерация `INDEX.md`, текст указателя (`self_docs_hint`).
- **Modify** `src/svarog_harness/runtime/executor.py` — расширить сигнатуру `context_files` в протоколе `AgentAdapter`.
- **Modify** `src/svarog_harness/runtime/agents/claude_code.py`, `opencode.py`, `codex.py` — принять `self_docs_path`, дописать указатель.
- **Modify** `src/svarog_harness/config/schema.py` — поле `self_docs: bool = True` в `ExternalExecutorConfig`.
- **Modify** `src/svarog_harness/runtime/agent_infra.py` — константа пути, хелпер `_add_launch_dir`, вызов в `prepare_launch`, проброс пути в `context_files`.
- **Create** `tests/test_self_docs.py` — юниты модуля self_docs + адаптеров + инфры.

---

## Task 1: Модуль self_docs (резолвер, стейджинг, INDEX, hint)

**Files:**
- Create: `src/svarog_harness/runtime/self_docs.py`
- Test: `tests/test_self_docs.py`

**Interfaces:**
- Consumes: ничего (stdlib).
- Produces:
  - `resolve_docs_root() -> Path | None`
  - `stage_self_docs(dest: Path, root: Path | None = None) -> Path | None`
  - `self_docs_hint(container_path: str) -> str`

- [ ] **Step 1: Написать падающие тесты**

Создать `tests/test_self_docs.py`:

```python
from pathlib import Path

import pytest

from svarog_harness.runtime import self_docs
from svarog_harness.runtime.self_docs import (
    resolve_docs_root,
    self_docs_hint,
    stage_self_docs,
)


def _fake_root(tmp_path: Path, *, with_agents: bool = True) -> Path:
    root = tmp_path / "repo"
    (root / "docs" / "adr").mkdir(parents=True)
    (root / "README.md").write_text("# Svarog\n\nкоманды\n", encoding="utf-8")
    if with_agents:
        (root / "AGENTS.md").write_text("# правила\n", encoding="utf-8")
    (root / "docs" / "adr" / "0001-first.md").write_text(
        "\n# ADR-0001. Первое решение\n\ntext\n", encoding="utf-8"
    )
    (root / "docs" / "adr" / "0016-exec.md").write_text(
        "# ADR-0016. External Agent Executor\n", encoding="utf-8"
    )
    return root


def test_resolve_docs_root_finds_repo() -> None:
    root = resolve_docs_root()
    assert root is not None
    assert (root / "README.md").is_file()
    assert (root / "docs" / "adr").is_dir()


def test_stage_copies_docs_and_builds_index(tmp_path: Path) -> None:
    root = _fake_root(tmp_path)
    dest = tmp_path / "staged"
    result = stage_self_docs(dest, root=root)
    assert result == dest
    assert (dest / "README.md").is_file()
    assert (dest / "AGENTS.md").is_file()
    assert (dest / "adr" / "0001-first.md").is_file()
    index = (dest / "INDEX.md").read_text(encoding="utf-8")
    assert "README.md" in index
    assert "AGENTS.md" in index
    # Заголовок ADR берётся из первой заголовочной строки.
    assert "ADR-0016. External Agent Executor" in index
    assert "adr/0016-exec.md" in index


def test_stage_without_agents_omits_it(tmp_path: Path) -> None:
    root = _fake_root(tmp_path, with_agents=False)
    dest = tmp_path / "staged"
    stage_self_docs(dest, root=root)
    assert not (dest / "AGENTS.md").exists()
    assert "AGENTS.md" not in (dest / "INDEX.md").read_text(encoding="utf-8")


def test_stage_returns_none_when_no_root(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setattr(self_docs, "resolve_docs_root", lambda: None)
    assert stage_self_docs(tmp_path / "staged") is None


def test_hint_mentions_index_and_path() -> None:
    hint = self_docs_hint("/opt/svarog-docs")
    assert "/opt/svarog-docs/INDEX.md" in hint
    assert "Svarog" in hint
```

- [ ] **Step 2: Прогнать — убедиться, что падает**

Run: `uv run pytest tests/test_self_docs.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'svarog_harness.runtime.self_docs'`

- [ ] **Step 3: Реализовать модуль**

Создать `src/svarog_harness/runtime/self_docs.py`:

```python
"""Self-документация Svarog для sandbox внешнего агента (ADR-0016).

Стейджит кураторскую копию доков (README, AGENTS.md, ADR) в launch-директорию
run'а и даёт агенту текст-указатель на смонтированный путь. Все три
external-адаптера читают её нативными Read/Grep — MCP reverse-tools доходят
только до Claude Code, поэтому механизм файловый, а не MCP.
"""

from __future__ import annotations

import shutil
from pathlib import Path

_ADR_DIRNAME = "adr"


def resolve_docs_root() -> Path | None:
    """Корень репо/пакета с документацией.

    Идёт вверх от этого модуля до директории, где есть и README.md, и
    docs/adr/. None — если маркеры не найдены (нестандартная упаковка):
    вызывающий мягко отключает self-docs.
    """
    for parent in Path(__file__).resolve().parents:
        if (parent / "README.md").is_file() and (parent / "docs" / _ADR_DIRNAME).is_dir():
            return parent
    return None


def _adr_title(md_path: Path) -> str:
    """Заголовок ADR из первой markdown-заголовочной строки; иначе имя файла."""
    for line in md_path.read_text(encoding="utf-8").splitlines():
        stripped = line.strip()
        if stripped.startswith("#"):
            return stripped.lstrip("#").strip()
    return md_path.stem


def _build_index(*, has_agents: bool, adr_files: list[Path]) -> str:
    lines = ["# Svarog — документация", ""]
    lines += ["## Использование", "- README.md — команды, флаги, фичи, quick start", ""]
    if has_agents:
        lines += [
            "## Правила репозитория",
            "- AGENTS.md — как работать с кодовой базой Svarog",
            "",
        ]
    if adr_files:
        lines.append("## Архитектурные решения (ADR)")
        for adr in adr_files:
            lines.append(f"- {_ADR_DIRNAME}/{adr.name} — {_adr_title(adr)}")
        lines.append("")
    return "\n".join(lines)


def stage_self_docs(dest: Path, root: Path | None = None) -> Path | None:
    """Скопировать доки в dest и сгенерить INDEX.md.

    root=None → резолвится автоматически. None-возврат — docs-root не найден
    (фича мягко отключается). Отсутствие AGENTS.md — не ошибка.
    """
    if root is None:
        root = resolve_docs_root()
    if root is None:
        return None
    dest.mkdir(parents=True, exist_ok=True)
    shutil.copy2(root / "README.md", dest / "README.md")
    has_agents = (root / "AGENTS.md").is_file()
    if has_agents:
        shutil.copy2(root / "AGENTS.md", dest / "AGENTS.md")
    adr_files = sorted((root / "docs" / _ADR_DIRNAME).glob("*.md"))
    if adr_files:
        (dest / _ADR_DIRNAME).mkdir(exist_ok=True)
        for adr in adr_files:
            shutil.copy2(adr, dest / _ADR_DIRNAME / adr.name)
    (dest / "INDEX.md").write_text(
        _build_index(has_agents=has_agents, adr_files=adr_files), encoding="utf-8"
    )
    return dest


def self_docs_hint(container_path: str) -> str:
    """Блок для контекст-файла агента: где искать доку про сам Svarog."""
    return (
        "# Документация Svarog\n\n"
        "На вопросы про сам Svarog (команды, фичи, архитектура) отвечай по "
        f"документации в {container_path}. Сначала прочитай "
        f"{container_path}/INDEX.md, затем нужный файл. Не выдумывай "
        "поведение Svarog — читай доку."
    )
```

- [ ] **Step 4: Прогнать — зелёный**

Run: `uv run pytest tests/test_self_docs.py -q`
Expected: PASS (5 тестов)

- [ ] **Step 5: Коммит**

```bash
git add src/svarog_harness/runtime/self_docs.py tests/test_self_docs.py
git commit -m "feat(self-docs): резолвер, стейджинг доков и INDEX для sandbox"
```

---

## Task 2: Указатель на доки в context_files адаптеров

**Files:**
- Modify: `src/svarog_harness/runtime/executor.py` (протокол `AgentAdapter.context_files`, ~строка 173)
- Modify: `src/svarog_harness/runtime/agents/claude_code.py` (`context_files`, ~строка 102)
- Modify: `src/svarog_harness/runtime/agents/opencode.py` (`context_files`, ~строка 65)
- Modify: `src/svarog_harness/runtime/agents/codex.py` (`context_files`, ~строка 75)
- Test: `tests/test_self_docs.py` (дополнить)

**Interfaces:**
- Consumes: `self_docs_hint(container_path)` из Task 1.
- Produces: `context_files(self, memory: str, skill_cards: str, self_docs_path: str | None = None) -> dict[str, str]` — единая сигнатура во всех адаптерах и протоколе. При `self_docs_path` не None в контекст-файл добавляется блок `self_docs_hint(self_docs_path)`. Дефолт `None` сохраняет обратную совместимость существующего вызова.

- [ ] **Step 1: Написать падающие тесты**

Дописать в `tests/test_self_docs.py`:

```python
from svarog_harness.runtime.agents.claude_code import ClaudeCodeAdapter
from svarog_harness.runtime.agents.codex import CodexAdapter
from svarog_harness.runtime.agents.opencode import OpencodeAdapter

_ADAPTERS = [
    (ClaudeCodeAdapter, "CLAUDE.md"),
    (OpencodeAdapter, ".config/opencode/AGENTS.md"),
    (CodexAdapter, "AGENTS.md"),
]


@pytest.mark.parametrize("adapter_cls, ctx_file", _ADAPTERS)
def test_context_files_include_hint_when_path_given(adapter_cls, ctx_file) -> None:
    files = adapter_cls().context_files("mem", "", "/opt/svarog-docs")
    assert "/opt/svarog-docs/INDEX.md" in files[ctx_file]


@pytest.mark.parametrize("adapter_cls, ctx_file", _ADAPTERS)
def test_context_files_omit_hint_when_none(adapter_cls, ctx_file) -> None:
    files = adapter_cls().context_files("mem", "")
    joined = files.get(ctx_file, "")
    assert "svarog-docs" not in joined
```

- [ ] **Step 2: Прогнать — убедиться, что падает**

Run: `uv run pytest tests/test_self_docs.py -k hint -q`
Expected: FAIL — `TypeError: context_files() takes 3 positional arguments but 4 were given`

- [ ] **Step 3: Расширить протокол**

В `src/svarog_harness/runtime/executor.py` заменить сигнатуру в протоколе `AgentAdapter` (строка ~173):

```python
    def context_files(
        self, memory: str, skill_cards: str, self_docs_path: str | None = None
    ) -> dict[str, str]:
        """Стартовые файлы контекста агента в state_dir.

        Ключ — относительный путь внутри state_dir → содержимое
        (CLAUDE.md / AGENTS.md); пусто — контекст не пишется. self_docs_path,
        если задан, — путь смонтированной документации Svarog для указателя.
        """
        ...
```

- [ ] **Step 4: claude_code.py — принять и дописать указатель**

В `src/svarog_harness/runtime/agents/claude_code.py` добавить импорт вверху рядом с прочими:

```python
from svarog_harness.runtime.self_docs import self_docs_hint
```

Заменить сигнатуру и добавить блок перед финальной сборкой:

```python
    def context_files(
        self, memory: str, skill_cards: str, self_docs_path: str | None = None
    ) -> dict[str, str]:
        """~/.claude/CLAUDE.md — глобальная память агента: контекст Svarog
        не попадает в workspace и не коммитится git-flow (ADR-0016 §4)."""
        sections: list[str] = []
        sections.append(
            "# Память\n\n"
            "Единственный источник истины по памяти — Svarog. Нативная auto-память "
            "Claude Code выключена. Чтобы что-то запомнить между запусками, вызывай "
            "MCP-tool `mcp__svarog__remember` (прочитать — `mcp__svarog__read_memory`); "
            "НЕ пиши факты в файлы через Write и НЕ веди свой ~/.claude/…/memory."
            + (f"\n\nТекущая память Svarog:\n\n{memory}" if memory else "")
        )
        sections.append(ask_user_guide("mcp__svarog__ask_user"))
        if skill_cards:
            sections.append(
                "# Скиллы Svarog\n\nПолное содержимое скилла — MCP-tool `read_skill`.\n\n"
                + skill_cards
            )
        if self_docs_path:
            sections.append(self_docs_hint(self_docs_path))
        if not sections:
            return {}
        return {"CLAUDE.md": "\n\n".join(sections) + "\n"}
```

- [ ] **Step 5: opencode.py — принять и дописать указатель**

В `src/svarog_harness/runtime/agents/opencode.py` добавить импорт:

```python
from svarog_harness.runtime.self_docs import self_docs_hint
```

Заменить сигнатуру и вставить блок перед `return`:

```python
    def context_files(
        self, memory: str, skill_cards: str, self_docs_path: str | None = None
    ) -> dict[str, str]:
        """Глобальные правила OpenCode: ~/.config/opencode/AGENTS.md."""
        sections: list[str] = []
        sections.append(
            "# Память\n\n"
            "Единственный источник истины по памяти — Svarog. Чтобы что-то "
            "запомнить между запусками, вызывай MCP-tool `svarog_remember` "
            "(прочитать — `svarog_read_memory`); НЕ пиши факты в файлы "
            "workspace и НЕ веди свою локальную память в ~/.local/share/opencode."
            + (f"\n\nТекущая память Svarog:\n\n{memory}" if memory else "")
        )
        sections.append(ask_user_guide("svarog_ask_user"))
        sections.append(
            "# Работа с файлами\n\n"
            "`write` перезаписывает файл ЦЕЛИКОМ. Чтобы дописать или изменить "
            "существующий файл, используй `edit`; если всё же пишешь через "
            "`write` — передавай ПОЛНОЕ итоговое содержимое файла."
        )
        if skill_cards:
            sections.append(f"# Скиллы Svarog\n\n{skill_cards}")
        if self_docs_path:
            sections.append(self_docs_hint(self_docs_path))
        return {".config/opencode/AGENTS.md": "\n\n".join(sections) + "\n"}
```

- [ ] **Step 6: codex.py — принять и дописать указатель**

В `src/svarog_harness/runtime/agents/codex.py` добавить импорт:

```python
from svarog_harness.runtime.self_docs import self_docs_hint
```

Заменить сигнатуру и добавить блок:

```python
    def context_files(
        self, memory: str, skill_cards: str, self_docs_path: str | None = None
    ) -> dict[str, str]:
        """~/.codex/AGENTS.md — глобальная инструкция Codex."""
        sections: list[str] = []
        if memory:
            sections.append(f"# Память Svarog\n\n{memory}")
        if skill_cards:
            sections.append(f"# Скиллы Svarog\n\n{skill_cards}")
        if self_docs_path:
            sections.append(self_docs_hint(self_docs_path))
        if not sections:
            return {}
        return {"AGENTS.md": "\n\n".join(sections) + "\n"}
```

- [ ] **Step 7: Прогнать — зелёный (включая существующие тесты адаптеров)**

Run: `uv run pytest tests/test_self_docs.py tests/test_agent_adapters.py -q`
Expected: PASS

- [ ] **Step 8: Коммит**

```bash
git add src/svarog_harness/runtime/executor.py src/svarog_harness/runtime/agents/ tests/test_self_docs.py
git commit -m "feat(self-docs): указатель на доки в context_files всех адаптеров"
```

---

## Task 3: Конфиг-флаг + монтирование в prepare_launch

**Files:**
- Modify: `src/svarog_harness/config/schema.py` (`ExternalExecutorConfig`, ~строка 335)
- Modify: `src/svarog_harness/runtime/agent_infra.py` (импорт, константа ~строка 36, `prepare_launch` ~строка 157, новый `_add_launch_dir` рядом с `_add_launch_file` ~строка 206)
- Test: `tests/test_self_docs.py` (дополнить)

**Interfaces:**
- Consumes: `stage_self_docs` (Task 1); `context_files(..., self_docs_path)` (Task 2); `ExternalExecutorConfig.self_docs`.
- Produces: `_add_launch_dir(self, name: str, container_path: str) -> str | None` в `ExternalAgentInfra`. Возвращает путь глазами агента (container_path в docker, host-путь локально) или `None`, если доки недоступны. `prepare_launch` при `cfg.self_docs=True` стейджит доки, монтирует в `/opt/svarog-docs` и пробрасывает путь в `context_files`.

- [ ] **Step 1: Написать падающие тесты**

Дописать в `tests/test_self_docs.py`:

```python
from svarog_harness.config.schema import ExternalExecutorConfig, RuntimeConfig
from svarog_harness.runtime import agent_infra as agent_infra_mod
from svarog_harness.runtime.agent_infra import ExternalAgentInfra
from svarog_harness.secrets import EnvSecretStore


def _codex_infra(tmp_path: Path, *, self_docs: bool = True) -> ExternalAgentInfra:
    cfg = ExternalExecutorConfig(image="img:1", adapter="codex", self_docs=self_docs)
    return ExternalAgentInfra(
        cfg,
        RuntimeConfig(),
        CodexAdapter(),
        EnvSecretStore(),
        state_root=tmp_path / ".svarog",
        docker_mode=True,
    )


def test_config_self_docs_defaults_on() -> None:
    assert ExternalExecutorConfig(image="img:1").self_docs is True


def test_prepare_launch_mounts_self_docs(tmp_path: Path) -> None:
    infra = _codex_infra(tmp_path)
    infra.prepare_launch("mem", "", cooperative=False)
    mounts = {container: (host, ro) for host, container, ro in infra.extra_mounts}
    assert "/opt/svarog-docs" in mounts
    host, ro = mounts["/opt/svarog-docs"]
    assert ro is True
    assert (host / "INDEX.md").is_file()
    agents_md = infra.state_dir / "AGENTS.md"
    assert "/opt/svarog-docs/INDEX.md" in agents_md.read_text(encoding="utf-8")


def test_prepare_launch_self_docs_disabled(tmp_path: Path) -> None:
    infra = _codex_infra(tmp_path, self_docs=False)
    infra.prepare_launch("mem", "", cooperative=False)
    assert "/opt/svarog-docs" not in {c for _, c, _ in infra.extra_mounts}
    assert "svarog-docs" not in (infra.state_dir / "AGENTS.md").read_text(encoding="utf-8")


def test_prepare_launch_degrades_without_docs_root(
    tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    monkeypatch.setattr(agent_infra_mod, "stage_self_docs", lambda dest: None)
    infra = _codex_infra(tmp_path)
    infra.prepare_launch("mem", "", cooperative=False)  # не должно падать
    assert "/opt/svarog-docs" not in {c for _, c, _ in infra.extra_mounts}
    assert "svarog-docs" not in (infra.state_dir / "AGENTS.md").read_text(encoding="utf-8")
```

- [ ] **Step 2: Прогнать — убедиться, что падает**

Run: `uv run pytest tests/test_self_docs.py -k "self_docs or prepare_launch or defaults" -q`
Expected: FAIL — `ValidationError` (нет поля `self_docs`) / `AttributeError` (`_add_launch_dir`).

- [ ] **Step 3: Добавить поле конфига**

В `src/svarog_harness/config/schema.py`, в `ExternalExecutorConfig`, после поля `model` (перед `relay_image`) добавить:

```python
    # Монтировать документацию Svarog (README, AGENTS.md, ADR) в sandbox
    # read-only (/opt/svarog-docs) и дать агенту указатель в контексте, чтобы
    # он отвечал на вопросы о самом Svarog по доке. False — не стейджить.
    self_docs: bool = True
```

- [ ] **Step 4: Импорт и константа пути в agent_infra.py**

В `src/svarog_harness/runtime/agent_infra.py` добавить импорт рядом с прочими `from svarog_harness.runtime...`:

```python
from svarog_harness.runtime.self_docs import stage_self_docs
```

Рядом с `_MCP_CONTAINER_PATH` (строка ~36) добавить:

```python
# Документация Svarog в контейнере (ro-mount): агент читает нативными
# Read/Grep, отвечая на вопросы о самом Svarog (спека self-docs).
_SELF_DOCS_CONTAINER_PATH = "/opt/svarog-docs"
```

- [ ] **Step 5: Хелпер `_add_launch_dir`**

В `src/svarog_harness/runtime/agent_infra.py` сразу после метода `_add_launch_file` (заканчивается `return container_path`, строка ~215) добавить:

```python
    def _add_launch_dir(self, name: str, container_path: str) -> str | None:
        """Стейдж доков в launch-dir и ro-mount директории.

        None — docs-root не найден (фича мягко отключается). Локально (тесты)
        монтирования нет — возвращается хостовый путь.
        """
        self._launch_dir.mkdir(parents=True, exist_ok=True)
        host_dir = self._launch_dir / name
        if stage_self_docs(host_dir) is None:
            return None
        if not self._docker_mode:
            return str(host_dir)
        self._extra_mounts.append((host_dir, container_path, True))
        return container_path
```

- [ ] **Step 6: Проброс в prepare_launch**

В `src/svarog_harness/runtime/agent_infra.py`, в начале `prepare_launch` заменить строку (~164):

```python
        state_files = dict(self._adapter.context_files(memory, skill_cards))
```

на:

```python
        docs_path: str | None = None
        if self._external_cfg.self_docs:
            docs_path = self._add_launch_dir("docs", _SELF_DOCS_CONTAINER_PATH)
        state_files = dict(self._adapter.context_files(memory, skill_cards, docs_path))
```

- [ ] **Step 7: Прогнать — зелёный**

Run: `uv run pytest tests/test_self_docs.py -q`
Expected: PASS (все тесты файла)

- [ ] **Step 8: Коммит**

```bash
git add src/svarog_harness/config/schema.py src/svarog_harness/runtime/agent_infra.py tests/test_self_docs.py
git commit -m "feat(self-docs): флаг executor.external.self_docs + ro-mount доков в sandbox"
```

---

## Task 4: Регресс, lint, mypy

**Files:** —

- [ ] **Step 1: Полный тестовый прогон**

Run: `uv run pytest -q`
Expected: PASS (существующие тесты `test_bridge.py`, `test_agent_adapters.py` не сломаны сменой сигнатуры `context_files` — дефолт `None` сохраняет совместимость).

- [ ] **Step 2: Lint + format + типы**

Run: `uv run ruff check . && uv run ruff format --check . && uv run mypy src`
Expected: без ошибок. При необходимости — `uv run ruff format .` и повторить.

- [ ] **Step 3: Финальный коммит (если правки от линтеров)**

```bash
git add -A
git commit -m "chore(self-docs): формат/линт"
```

---

## Self-Review

**Spec coverage:**
- Файловый механизм + mount `:ro` `/opt/svarog-docs` → Task 3. ✓
- Резолвер + стейдж-копия + мягкая деградация → Task 1 (+ degrade-тест Task 3). ✓
- `INDEX.md` с ADR-заголовками → Task 1. ✓
- Discovery через `context_files` всех адаптеров → Task 2. ✓
- Флаг `executor.external.self_docs` (default on) → Task 3. ✓
- Non-goals (нет `TASK.md`, нет RAG, нет MCP-tool, нет нативного loop) → соблюдены: стейджим только README/AGENTS/adr, плоские файлы, файловый механизм, external-инфра. ✓

**Placeholder scan:** плейсхолдеров нет — во всех шагах полный код и команды.

**Type consistency:** `context_files(memory, skill_cards, self_docs_path=None)` — единая сигнатура в протоколе (Task 2 Step 3) и трёх адаптерах (Steps 4-6), вызывается с `docs_path` в Task 3 Step 6. `stage_self_docs(dest, root=None) -> Path | None`, `resolve_docs_root() -> Path | None`, `self_docs_hint(container_path) -> str`, `_add_launch_dir(name, container_path) -> str | None` — согласованы между задачами. Тесты Task 3 монтируют `codex` (mcp=False), поэтому `prepare_launch` не обращается к `bridge`/сети и работает без `start()`.
