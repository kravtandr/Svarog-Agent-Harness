# Группа документы и изображения (S27–S29), opencode

Образ f77365c7a787: pandoc 2.17, tesseract 5.3 (rus+eng), opencode 1.18.9.
`svarog doctor` → document-tools: ok (markitdown на хосте).

## S27 read_document XLSX — FAIL (галлюцинация через subagent)
XLSX (выручка-морж-7742) создан. `svarog_read_document` вызван, мост вернул ok,
НО агент делегировал чтение subagent-task'у (subagent_type=general), которая
выдала ПЕРЕВРАННЫЙ результат: «Revenue (Gross Margin 7742): 45000» — маркер
«выручка-морж-7742» перевран в «Gross Margin 7742», grep маркера = 0. Это
галлюцинация содержимого (Watch 1 S27). Механика моста рабочая (тул вызывается,
возвращает ok), проблема — агент гонит чтение через task-subagent, а не напрямую,
и теряет точность/галлюцинирует. Контраст с прошлым прогоном (24.07): там
единый вызов read_document → точный маркер. Кандидат: промпт доводит агента до
прямого вызова, а не делегирования subagent'у.

## S28 RTF через pandoc — PASS (после 1 перепрогона)
Идеальная траектория: `svarog_read_document(notes/legacy.rtf)` → ошибка со
списком поддерживаемых форматов (RTF не покрыт) → bash `pandoc notes/legacy.rtf
-o notes/legacy.md` → нативный Read → маркер SVAROG-MARKER-4217 назван верно.
Error-путь моста + bash-конвертер образа работают end-to-end. Первый прогон
оборвался инфрафлейком «стрим без result» (та же траектория до обрыва).

## S29 read_image vision — ЖЁЛТЫЙ/инфра (модель)
Vision-модель google/gemini-2.0-flash-001 через OpenRouter: run упал на старте
(0 токенов, «внешний агент завершился с кодом 1») — gemini-flash не поднялась
через opencode-прокси с этим base_url. Инфра-FAIL модели (как спека S29:
«модель без vision — мусор, смени модель, а не считай багом агента»). Не
удалось подобрать рабочую vision-модель в бюджет прогона. Совпадает с
историческим ЖЁЛТЫМ S29 на opencode (24.07: MCP-клиент не рендерит image-блок;
зеркало claude-code — ЗЕЛЁНЫЙ). Слой моста в этом прогоне не верифицирован
(модель не стартовала); оставлен как ЖЁЛТЫЙ/инфра.

## Итог группы
Мост `svarog_read_document` работает (тул вызывается, возвращает ok/error с
подсказкой). bash-конвертеры образа (pandoc) работают (S28 PASS). Проблема S27 —
поведенческая (делегирование subagent'у с потерей точности), S29 — инфра
(модель). Vision на opencode остаётся открытым (как и в 24.07).
